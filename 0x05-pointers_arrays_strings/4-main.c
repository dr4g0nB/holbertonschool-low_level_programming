#include "holberton.h"

/**
 *
 *
 */

void print_rev(char *s)
{
	int in;

	int count(char *str)
	{
		for (in = 0; str[in] != '\0'; str++)
		{}
		return (i);
	}

	n = count(str);
	for (in = n-1; in >= 0; 1--)
	{
		_putchar(str[in]);
	}
}
