#include <stdio.h>

/**
 * main - Prints the number of arguments passed into it.
 * @argc: number of things.
 * @argv: array.
 * Return: 0.
 */

int main(int argc, char *argv[])
{
	int i;

	/**printf("%d\n", argc);*/

	if()
		printf("%s\n", argv[]);
		printf("%d\n", argc);
	return(0);
}
