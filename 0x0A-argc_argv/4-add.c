#include <stdio.h>

/**
 * main - adds positives numbers.
 * @argc: .
 * @argv: .
 * Return: .
 */

int main(int argc, char *argv[])
{
	int d;

	if(argc == '\0')
	{
		printf("0\n");
	}
	if (d >= 'a' && d <= 'z')
	{
		printf("Error\n");
	}
	/**else
	{
		printf();
	}*/
	return (1);
}
